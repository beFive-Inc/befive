const formCheck = {
    form: document.querySelectorAll('form'),
    el: null,

    init() {
        this.form.forEach((element) => {

        })
        console.log(this.form);
    },

    check() {

    }
}

formCheck.init();
